name: appsfactory_task
description: AppsFactory home task.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  redux: ^5.0.0
  flutter_redux: ^0.8.2
  async: ^2.8.1
  transparent_image: ^2.0.0
  cached_network_image: ^3.1.0
  dio: ^4.0.0
  cupertino_icons: ^1.0.3
  url_launcher: ^6.0.9
  fluttertoast: ^8.0.8
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  path_provider: ^2.0.5
  get_it: ^7.2.0



dev_dependencies:
  hive_generator: ^1.1.1
  build_runner: ^2.1.4
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter
  mockito:

flutter:
  uses-material-design: true
  assets:
    - assets/